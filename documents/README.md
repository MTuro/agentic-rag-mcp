# Local RAG Documents

Markdown files in this directory are loaded as standalone documents before they
are divided into smaller, overlapping text chunks. Embeddings and retrieval are
intentionally not part of this milestone.
